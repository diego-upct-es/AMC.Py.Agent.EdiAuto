# ──────────────────────────────────────────────────────────
# AMC.Py.Agent.EdiAuto — Dependencias Python
# ──────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# HTTP client async
httpx==0.28.1

# Extracción de texto de adjuntos PDF
pypdf==6.16.2

# Base de datos PostgreSQL async
asyncpg==0.30.0

# Validación de datos y configuración
pydantic==2.10.3
pydantic-settings==2.7.0

# Azure Service Bus (cola de mensajes — mismo que usan otros microservicios AMC)
azure-servicebus==7.12.3

# Azure Key Vault (recuperación de secretos, ej. DATABASE_URL en producción)
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0

# Logging estructurado
structlog==24.4.0

# Reintentos automáticos
tenacity==9.0.0

# Variables de entorno
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-httpx==0.35.0
